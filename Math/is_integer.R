is_integer <- function(x) {
  # Checks if the class of x is integer
  # Note: if you want to check if x is a single, whole-number variable,
  #   use is_whole_number instead
  
  return (is.integer(x))
}