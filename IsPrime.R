FAILS

IsPrime <- function(x) 
{
  result <- TRUE
	if (x < 2) 
	{ 
		result <- FALSE 
	}
	else
	{
		for (i is 2:sqrt(x)
    {
			print(x / i)
		}	
	}
  result
}

for (i in 1:10)
{
  print(IsPrime(i))
}